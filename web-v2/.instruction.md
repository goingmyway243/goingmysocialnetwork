You are an expert in TypeScript, Angular, and scalable web application development. You write functional, maintainable, performant, and accessible code following Angular and TypeScript best practices.

## UI Component Library - MANDATORY
**ALWAYS use PrimeNG for all UI components. This is REQUIRED and NON-NEGOTIABLE.**

### PrimeNG Usage Requirements
- **NEVER use native HTML form elements** - always use PrimeNG equivalents
- Import required PrimeNG modules in component imports array
- Use PrimeNG theme: `primeng/resources/themes/lara-dark-blue/theme.css`
- Always import PrimeIcons: `primeicons/primeicons.css`
- Import PrimeNG core styles: `primeng/resources/primeng.css`

### Required PrimeNG Components by Use Case
**Forms (MANDATORY - Never use native elements):**
- Text input: `<input pInputText>` from `primeng/inputtext`
- Password: `<p-password>` from `primeng/password`
- Checkbox: `<p-checkbox>` from `primeng/checkbox`
- Radio: `<p-radioButton>` from `primeng/radiobutton`
- Dropdown: `<p-dropdown>` from `primeng/dropdown`
- Multi-select: `<p-multiSelect>` from `primeng/multiselect`
- Calendar: `<p-calendar>` from `primeng/calendar`
- Textarea: `<textarea pInputTextarea>` from `primeng/inputtextarea`
- Number: `<p-inputNumber>` from `primeng/inputnumber`
- Slider: `<p-slider>` from `primeng/slider`

**Buttons:**
- Button: `<p-button>` from `primeng/button`
- Split button: `<p-splitButton>` from `primeng/splitbutton`
- Speed dial: `<p-speedDial>` from `primeng/speeddial`

**Data Display:**
- Table: `<p-table>` from `primeng/table`
- Data view: `<p-dataView>` from `primeng/dataview`
- Card: `<p-card>` from `primeng/card`
- Panel: `<p-panel>` from `primeng/panel`
- Accordion: `<p-accordion>` from `primeng/accordion`
- Tabs: `<p-tabView>` from `primeng/tabview`

**Overlays:**
- Dialog: `<p-dialog>` from `primeng/dialog`
- Sidebar: `<p-sidebar>` from `primeng/sidebar`
- Tooltip: `pTooltip` directive from `primeng/tooltip`
- Confirm dialog: `<p-confirmDialog>` from `primeng/confirmdialog`

**Messages:**
- Toast: `<p-toast>` from `primeng/toast`
- Message: `<p-message>` from `primeng/message`
- Messages: `<p-messages>` from `primeng/messages`

**Navigation:**
- Menu bar: `<p-menubar>` from `primeng/menubar`
- Menu: `<p-menu>` from `primeng/menu`
- Breadcrumb: `<p-breadcrumb>` from `primeng/breadcrumb`
- Steps: `<p-steps>` from `primeng/steps`

### PrimeNG Styling with Liquid Glass
- **AVOID `::ng-deep`** - it's deprecated and should only be used as a last resort
- **PREFERRED APPROACH**: Define PrimeNG component styles in global `styles.css` targeting component classes directly (e.g., `.p-button`, `.p-checkbox`, `.p-password`)
- Use `styleClass` prop to apply custom CSS classes to component wrapper
- Use `inputStyleClass` prop to apply custom classes to input elements
- Use `panelStyleClass`, `contentStyleClass` where available for nested elements
- Override default PrimeNG styles in global stylesheet to match liquid glass aesthetic
- Maintain WCAG AA contrast ratios when applying glassmorphism effects

**Example Pattern:**
```typescript
// Component HTML
<p-button styleClass="submit-btn" label="Submit" />
<p-password inputStyleClass="form-input" styleClass="custom-password" />

// Global styles.css
.p-button.submit-btn {
  background: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(10px);
  border-radius: 12px;
}

.p-password input.form-input {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
}
```

### PrimeNG with Reactive Forms
- All PrimeNG form components support `formControlName`
- Use `[binary]="true"` on checkboxes for boolean values
- Access validation with `ng-invalid` and `ng-dirty` classes
- PrimeNG automatically handles form control states

## TypeScript Best Practices
- Use strict type checking
- Prefer type inference when the type is obvious
- Avoid the `any` type; use `unknown` when type is uncertain
## Angular Best Practices
- Always use standalone components over NgModules
- Must NOT set `standalone: true` inside Angular decorators. It's the default in Angular v20+.
- Use signals for state management
- Implement lazy loading for feature routes
- Do NOT use the `@HostBinding` and `@HostListener` decorators. Put host bindings inside the `host` object of the `@Component` or `@Directive` decorator instead
- Use `NgOptimizedImage` for all static images.
  - `NgOptimizedImage` does not work for inline base64 images.
## Accessibility Requirements
- It MUST pass all AXE checks.
- It MUST follow all WCAG AA minimums, including focus management, color contrast, and ARIA attributes.
### Components
- Keep components small and focused on a single responsibility
- Use `input()` and `output()` functions instead of decorators
- Use `computed()` for derived state
- Set `changeDetection: ChangeDetectionStrategy.OnPush` in `@Component` decorator
- Prefer inline templates for small components
- Prefer Reactive forms instead of Template-driven ones
- Do NOT use `ngClass`, use `class` bindings instead
- Do NOT use `ngStyle`, use `style` bindings instead
- When using external templates/styles, use paths relative to the component TS file.
## State Management
- Use signals for local component state
- Use `computed()` for derived state
- Keep state transformations pure and predictable
- Do NOT use `mutate` on signals, use `update` or `set` instead
## Templates
- Keep templates simple and avoid complex logic
- Use native control flow (`@if`, `@for`, `@switch`) instead of `*ngIf`, `*ngFor`, `*ngSwitch`
- Use the async pipe to handle observables
- Do not assume globals like (`new Date()`) are available.
## Services
- Design services around a single responsibility
- Use the `providedIn: 'root'` option for singleton services
- Use the `inject()` function instead of constructor injection
## UI Design
- Follow liquid glass design principles: use frosted glass effects with `backdrop-filter: blur()`
- Apply semi-transparent backgrounds with `rgba()` or `hsla()` colors
- Maintain minimum 4.5:1 contrast ratio for text over glass effects for WCAG AA compliance
- Use smooth gradients and soft shadows for depth
- Ensure glass morphism effects do not impair readability or accessibility
- Test all glass effects with high contrast mode enabled